name: ERP Client Platform V1 Customer Permission Certification

on:
  push:
    branches:
      - feature/erp-client-platform-v1
    paths:
      - "access/**"
      - "roles/**"
      - "user/**"
      - "auth/tests/**"
      - "config/**"
      - "requirements.txt"
      - ".github/workflows/erp-client-platform-v1-certification.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: erp-client-platform-v1-auth-${{ github.ref }}
  cancel-in-progress: false

jobs:
  customer-enterprise-permissions:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:14.4
        env:
          POSTGRES_DB: Auth
          POSTGRES_USER: Auth
          POSTGRES_PASSWORD: auth_test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U Auth -d Auth"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      DJANGO_SECRET_KEY: feature-ci-not-production
      DATABASE_URL: postgresql://Auth:auth_test_password@127.0.0.1:5432/Auth
      AUTH_TEST_POSTGRES_OPTIONS: -c search_path=public,"Auth","AuthRuntime"
      DEVELOPMENT_MODE: "True"
      AUTH_EMAIL_DEFERRED_EXTERNAL: "true"
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt
      - name: Dependency integrity
        run: python -m pip check
      - name: Django system check
        run: python manage.py check
      - name: Migration drift check
        run: python manage.py makemigrations --check --dry-run
      - name: Migration plan
        run: python manage.py migrate --plan
      - name: Apply migrations
        run: python manage.py migrate --noinput
      - name: Auth database contract regression test
        run: |
          python - <<'PY'
          import os
          import unittest
          import django

          os.environ.setdefault("DJANGO_SETTINGS_MODULE", "config.settings")
          django.setup()
          suite = unittest.defaultTestLoader.loadTestsFromName(
              "user.test_database_checks.AuthDatabaseConfigurationCheckTests."
              "test_django_temporary_database_is_allowed_only_during_test_execution"
          )
          result = unittest.TextTestRunner(verbosity=2).run(suite)
          raise SystemExit(0 if result.wasSuccessful() else 1)
          PY
      - name: Customer enterprise permission seed tests
        run: python manage.py test access.test_customer_enterprise_permissions --verbosity 2
      - name: JobCron Product Import permission seed tests
        run: python manage.py test access.test_jobcron_product_import_permissions --verbosity 2
      - name: Mobile session contract tests
        run: python manage.py test auth.tests.test_mobile_session_revocation --verbosity 2
